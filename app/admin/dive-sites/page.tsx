import { requireAdmin } from '@/lib/auth'
import { getAdminDiveSites } from '@/lib/data'
import { AdminHeader } from '@/components/admin/admin-ui'
import { CmsManager, type CmsField } from '@/components/admin/cms-manager'
const fields:CmsField[]=[{name:'name',label:'Name',required:true},{name:'slug',label:'Slug',required:true},{name:'description',label:'Description',kind:'textarea'},{name:'long_description',label:'Long description',kind:'textarea'},{name:'site_type',label:'Site type'},{name:'depth_min',label:'Minimum depth',kind:'number'},{name:'depth_max',label:'Maximum depth',kind:'number'},{name:'difficulty',label:'Difficulty'},{name:'current_level',label:'Current level'},{name:'marine_life',label:'Marine life',kind:'textarea'},{name:'journey_time',label:'Journey time'},{name:'media_file',label:'Featured image upload',kind:'file'},{name:'featured_image',label:'Featured image URL'},{name:'gallery',label:'Gallery URLs',kind:'textarea'},{name:'latitude',label:'Latitude',kind:'number'},{name:'longitude',label:'Longitude',kind:'number'},{name:'featured',label:'Featured',kind:'checkbox'},{name:'active',label:'Active',kind:'checkbox'},{name:'sort_order',label:'Sort order',kind:'number'}]
export default async function Page(){await requireAdmin();const items=await getAdminDiveSites();return <div><AdminHeader title="Dive Sites" description="Create and maintain South Ari dive sites."/><CmsManager entity="dive_sites" items={items as unknown as (Record<string,unknown>&{id:string})[]} fields={fields} titleKey="name"/></div>}
